{
    "name": "preparedQueryOptionsTests",
    "description": "JS Library for managing OData query parameter building and parsing",
    "version": "1.2.0",
    "private": true,
    "scripts": {
        "test": "mocha --reporter spec tests/*.js"
    },
    "engines": {
        "node": "0.10.x",
        "npm": "1.3.x"
    },
    "devDependencies": {
        "chai": "*",
        "mocha": "*"
    }
}